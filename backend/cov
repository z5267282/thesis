#!/bin/zsh

# note that the coverage depends on command line arg given

# not right coverage:
# pytest --cov=src --cov-report=html --cov-config=.coveragerc

pytest --cov=src --cov-report=html --cov-branch
